						git shell

	Recupéré ancien commit :

selectionner le commit regarder la liste de chiffre et les lettres à côté et écrire :

- git status 
- git reset --hard "liste de chiffre"


	supprimer le dernier commit 

- git push origin master

Faire un commit sans bug 

-commit sur sa branche
- branche master : git pull origin master
- sur sa branche : git rebase master
- sur master: git merge <sa brache>

